#*******************************************
# MacVersion = 1.0
# MacDescription = Encrypt/Decrypt all hints
# MacAuthor = javapgmr
# MacFileName = encryptdecrpythints.gsk
# MacUrl =
#*******************************************
GoTo Position=Top
While not($_Eol)
  $Hints=HTML("Hints","E")
  $Hints = Replace("<br>","",$Hints)
  $Hints = Replace("<oe>","",$Hints)
  $Hints = Replace("","",$Hints)
  $Hints = Replace("","",$Hints)
  $d_Hints = $Hints
GOTO position=Next
EndWhile